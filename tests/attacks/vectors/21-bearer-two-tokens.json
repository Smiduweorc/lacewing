{
	"name": "Authorization header with two tokens",
	"description": "An Authorization header smuggling two tokens, hoping the server picks the attacker's. The strict RFC 6750 parser accepts exactly one token and rejects this.",
	"attacks": "parser confusion",
	"reference": "https://datatracker.ietf.org/doc/html/rfc6750#section-2.1",
	"relatedRequirements": ["LW-http.2"],
	"expectedError": "BearerParseFailed",
	"expectedCode": "BEARER_PARSE_FAILED",
	"target": "bearer",
	"recipe": { "kind": "bearer", "header": "Bearer aaaaaaaaaaaa bbbbbbbbbbbb" }
}
