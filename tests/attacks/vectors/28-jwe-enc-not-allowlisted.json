{
	"name": "JWE enc outside the profile allowlist",
	"description": "A genuinely valid encryption under the profile's own dir key, but using A128CBC-HS256 (which the same 32-byte key can also serve) while the profile allowlists only A256GCM. The header never chooses: rejected before decryption.",
	"attacks": "content-encryption downgrade to an algorithm the profile did not allowlist",
	"reference": "https://datatracker.ietf.org/doc/html/rfc8725#section-3.1",
	"relatedRequirements": ["8725-3.1.1", "LW-enc.2"],
	"expectedError": "AlgorithmNotAllowed",
	"expectedCode": "ALGORITHM_NOT_ALLOWED",
	"target": "decrypt",
	"recipe": { "kind": "jweEncrypted", "enc": "A128CBC-HS256" }
}
